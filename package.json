{
  "name": "scale-monitor",
  "version": "0.0.1",
  "description": "Simple script to display scale output on an external monitor via an RS-232 Serial I/O port.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Ross",
  "license": "ISC",
  "dependencies": {
    "@serialport/parser-readline": "^1.0.5",
    "chalk": "^2.4.1",
    "express": "^4.16.3",
    "opts": "^1.2.6",
    "serialport": "^6.2.1",
    "socket.io": "^2.1.1"
  }
}
